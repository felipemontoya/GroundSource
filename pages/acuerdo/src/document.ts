/**
 * The one document this page talks to.
 *
 * This page is not a document browser: it exists for the 24 November 2016
 * Final Agreement and nothing else, so the slug is fixed here rather than
 * read from the address bar. It must match the slug the document was
 * ingested under in the environment the build points at — set it to the
 * production slug once the agreement is ingested there.
 */
export const SOURCE_SLUG = "acuerdo-final-jep-2016";
